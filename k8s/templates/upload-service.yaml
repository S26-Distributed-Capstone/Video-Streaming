apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-upload
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-upload
  ports:
    - port: 8080
      targetPort: 8080
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.uploadNodePort }}
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-upload
spec:
  replicas: {{ .Values.uploadService.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-upload
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-upload
    spec:
      {{- include "video-streaming.pod-scheduling" .Values.scheduling.infra | nindent 6 }}
      initContainers:
        {{- include "video-streaming.wait-for" (dict "name" "rabbitmq" "host" (printf "%s-rabbitmq" .Release.Name) "port" "5672") | nindent 8 }}
        {{- include "video-streaming.wait-for" (dict "name" "postgres" "host" (printf "%s-postgres" .Release.Name) "port" "5432") | nindent 8 }}
      containers:
        - name: upload-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_MODE
              value: "upload"
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-config
            - secretRef:
                name: {{ .Release.Name }}-secrets
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: {{ .Values.uploadService.resources.requests.memory | quote }}
              cpu: {{ .Values.uploadService.resources.requests.cpu | quote }}
            limits:
              memory: {{ .Values.uploadService.resources.limits.memory | quote }}
              cpu: {{ .Values.uploadService.resources.limits.cpu | quote }}
