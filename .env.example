# MinIO Object Storage
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key
MINIO_BUCKET_NAME=uploads
MINIO_REGION=us-east-1

CHUNK_DURATION_SECONDS=6

# PostgreSQL
# Docker-compose variables
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=videostreaming
# Application variables (keep in sync with above)
PG_URL=jdbc:postgresql://localhost:5432/videostreaming
PG_USER=your_postgres_user
PG_PASSWORD=your_postgres_password
PG_DB=videostreaming

MACHINE_ID=dev-machine-1

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=your_rabbitmq_user
RABBITMQ_PASS=your_rabbitmq_password
RABBITMQ_VHOST=/

RABBITMQ_EXCHANGE=upload.events

RABBITMQ_STATUS_QUEUE=upload.status.queue
RABBITMQ_STATUS_BINDING=upload.status.*

# Task queue is reserved for downstream workers (not used by upload/status services)
RABBITMQ_TASK_QUEUE=upload.task.queue
RABBITMQ_TASK_BINDING=upload.task.*

# Status service port (used by upload service to build WS URLs)
STATUS_PORT=8081

# Processing service port and worker pool size
PROCESSING_PORT=8082
WORKER_POOL_SIZE=4

# Note: SERVICE_MODE, RABBITMQ_STATUS_QUEUE, and RABBITMQ_STATUS_BINDING
# are set per-service in docker-compose.yaml and should NOT be set here,
# as they differ between the upload, status, and processing services.